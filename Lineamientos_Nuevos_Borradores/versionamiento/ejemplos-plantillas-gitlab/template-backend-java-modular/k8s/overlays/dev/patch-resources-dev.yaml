apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-nombre-sistema
spec:
  template:
    spec:
      containers:
        - name: api-nombre-sistema
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "250m"
              memory: "512Mi"
