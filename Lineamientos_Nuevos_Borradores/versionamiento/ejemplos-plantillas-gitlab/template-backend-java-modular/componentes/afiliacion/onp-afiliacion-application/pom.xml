<?xml version="1.0" encoding="UTF-8"?>
<!--
  Casos de uso transaccionales y orquestación (LIN-DEV-JAVA-001 §14.1). Depende del
  -domain propio y de onp-common-domain. Excepcionalmente puede depender del -domain
  de OTRO componente para consultas en memoria dentro de la misma JVM — ver regla de
  gobernanza #2 en LIN-DEV-JAVA-001 §14.1 (nunca del -application, -infrastructure o
  -api de otro componente).
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>pe.gob.onp.template</groupId>
        <artifactId>onp-template</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../../../pom.xml</relativePath>
    </parent>

    <artifactId>onp-afiliacion-application</artifactId>
    <packaging>jar</packaging>
    <name>onp-afiliacion-application</name>

    <dependencies>
        <dependency>
            <groupId>pe.gob.onp.template</groupId>
            <artifactId>onp-afiliacion-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>pe.gob.onp.template</groupId>
            <artifactId>onp-common-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.coverage.minimum.domain}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
