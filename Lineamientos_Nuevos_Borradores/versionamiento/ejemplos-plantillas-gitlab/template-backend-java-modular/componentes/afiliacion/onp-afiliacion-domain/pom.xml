<?xml version="1.0" encoding="UTF-8"?>
<!--
  Anillo interior hexagonal (LIN-DIS-001 §2.2, LIN-DEV-JAVA-001 §14.1). Reglas de negocio
  puras e interfaces de puertos. Depende ÚNICAMENTE de onp-common-domain — cero Spring,
  cero JPA, cero Jackson. Si esta clase importa algo de org.springframework.* o
  jakarta.persistence.*, la pureza hexagonal está rota (señal de alerta penalizable,
  LIN-DEV-JAVA-001 §14.1).
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>pe.gob.onp.template</groupId>
        <artifactId>onp-template</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../../../pom.xml</relativePath>
    </parent>

    <artifactId>onp-afiliacion-domain</artifactId>
    <packaging>jar</packaging>
    <name>onp-afiliacion-domain</name>

    <dependencies>
        <dependency>
            <groupId>pe.gob.onp.template</groupId>
            <artifactId>onp-common-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.coverage.minimum.domain}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
